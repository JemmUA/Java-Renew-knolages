package hilelll.git;

public class Main {
/**
    git remote add origin https://github.com/JemmUA/tryGit.git

    git init
    git add .
    git add a
    git fetch
    git push origin develop
    git pull origin develop
    git checkout - (на попередню гілку)
    git checkout (перейти на гілку)
    git checkout -b (створити та перейти на гілку)
    git branch (поточна гілка)
    git branch xxx(створити нову гілку)
    git restore .\src\a (відновити файл а)
    git restore . (відновити всі файли)
    git commit -m "file with changes brabch" (коміт)
    git commit -am "file with changes brabch" (коміт з індексфцією
    git merge main
    git log -5 --oneline
    git merge -- abort

            configurations
    git config --system -- list
    git config --global -- list
    git config --local -- list

            stash
    git stash
    git stash save develop
    git stash list
    git stash apply 0
    git stash pop 0
    git stash drop 0
            --index

    git commit --amend (для останнього комміта)

    cherry-pick
    git cherry-pick
    git cherry-pick main develop
    git log -11 --oneline
    git cherry-pick eff7736 --no-commit


*/

}
